import { Assistant, Lexend, Mulish } from 'next/font/google';

export const assistant = Assistant({ subsets: ['latin'], weight: ['400', '800'] });
export const lexend = Lexend({ subsets: ['latin'], weight: ['400', '900'] });
export const mulish = Mulish({ subsets: ['latin'], weight: ['500', '900'] });
